export class Product {
  constructor(
    public _id: string,
    public category: String,
    public title: String,
    public price: Number,
    public image: String,
    public updatedAt: String
  ) {
  }
}
